import { Counter } from "../../components/Counter";
import { MainTemplate } from "../../Templates/MainTemplate";

export const Home = () => {
  return (
    <MainTemplate>
      <Counter />
    </MainTemplate>
  );
};
